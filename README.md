# DHT22_MQTT_Raspi
Simple MQTT publishing on a raspi for the DHT22 temperature sensor

- The sensor can be easily replaced with anything you want to publish via mqtt.
- Configuration via simple variables
- Variable delays between publishes depending on if sensor read was successful or not
